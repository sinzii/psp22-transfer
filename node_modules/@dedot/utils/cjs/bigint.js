"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.bnToU8a = exports.bnToHex = exports.bnAbs = exports.bnMin = exports.bnMax = void 0;
const hex_js_1 = require("./hex.js");
/**
 * Return the bigger bigint value
 */
function bnMax(a, b) {
    return a > b ? a : b;
}
exports.bnMax = bnMax;
/**
 * Return the smaller bigint value
 */
function bnMin(a, b) {
    return a > b ? b : a;
}
exports.bnMin = bnMin;
/**
 * Return the absolute value of a bigint
 */
function bnAbs(n) {
    return n < 0n ? -n : n;
}
exports.bnAbs = bnAbs;
/**
 * Convert bigint to hex
 */
function bnToHex(n) {
    const hex = bnAbs(n).toString(16);
    return `0x${hex.length % 2 ? `0${hex}` : hex}`;
}
exports.bnToHex = bnToHex;
/**
 * Convert bigint to Uint8Array
 */
function bnToU8a(n) {
    return (0, hex_js_1.hexToU8a)(bnToHex(n));
}
exports.bnToU8a = bnToU8a;
