import { CommandModule } from 'yargs';
type Args = {
    contract?: string;
    output?: string;
    metadata?: string;
    dts?: boolean;
    subpath?: boolean;
};
export declare const typink: CommandModule<Args, Args>;
export {};
