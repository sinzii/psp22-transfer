import { CommandModule } from 'yargs';
type Args = {
    wsUrl?: string;
    output?: string;
    chain?: string;
    dts?: boolean;
    subpath?: boolean;
    wasm?: string;
    metadata?: string;
};
export declare const chaintypes: CommandModule<Args, Args>;
export {};
