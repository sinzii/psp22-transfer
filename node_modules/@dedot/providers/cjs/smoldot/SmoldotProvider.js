"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SmoldotProvider = void 0;
const utils_1 = require("@dedot/utils");
const index_js_1 = require("../base/index.js");
/**
 * @name SmoldotProvider
 */
class SmoldotProvider extends index_js_1.SubscriptionProvider {
    #chain;
    constructor(chain) {
        super();
        this.setChain(chain);
    }
    chain() {
        (0, utils_1.assert)(this.#chain, 'Smoldot chain is not available');
        return this.#chain;
    }
    setChain(chain) {
        (0, utils_1.assert)(this.status === 'disconnected', 'Smoldot chain cannot be changed while connected');
        this.#chain = chain instanceof Promise ? chain : Promise.resolve(chain);
    }
    async connect() {
        await this.chain(); // make sure the chain promise is completely resolved
        this._setStatus('connected');
        this.#startPullingResponses();
        return this;
    }
    #startPullingResponses() {
        (async () => {
            while (true) {
                if (this.status === 'disconnected')
                    break;
                try {
                    const chain = await this.chain();
                    const rawResponse = await chain.nextJsonRpcResponse();
                    this._onReceiveResponse(rawResponse);
                }
                catch (e) {
                    this.emit('error', e);
                }
            }
        })();
    }
    /**
     * Disconnect the provider & remove the chain
     *
     * To reconnect again, make sure set a new `smoldot.Chain` instance
     * via `setChain` first & call `connect`
     */
    async disconnect() {
        this._setStatus('disconnected');
        (await this.chain()).remove();
        this.#chain = undefined;
        this._cleanUp();
    }
    async doSend(request) {
        (0, utils_1.assert)(this.status === 'connected', 'The provider is not connected, please call .connect() first.');
        const chain = await this.chain();
        chain.sendJsonRpc(JSON.stringify(request));
    }
}
exports.SmoldotProvider = SmoldotProvider;
