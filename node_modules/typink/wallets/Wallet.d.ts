export interface WalletOptions {
    id: string;
    name: string;
    logo: string;
}
export declare abstract class Wallet<Options extends WalletOptions = WalletOptions> {
    options: Options;
    constructor(options: Options);
    get id(): string;
    get name(): string;
    get logo(): string;
    get version(): string | undefined;
    get injectedWeb3(): Record<string, import("../pjs-types.js").InjectedWindowProvider>;
    get injectedProvider(): import("../pjs-types.js").InjectedWindowProvider;
    get ready(): boolean;
    get installed(): boolean;
    waitUntilReady(): Promise<void>;
}
