import { Wallet, WalletOptions } from './Wallet.js';
export interface ExtensionWalletOptions extends WalletOptions {
    installUrl: string;
    websiteUrl?: string;
}
export declare class ExtensionWallet extends Wallet<ExtensionWalletOptions> {
    get installUrl(): string;
    get websiteUrl(): string | undefined;
    get installed(): boolean;
}
