"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.result = void 0;
const mod_js_1 = require("../common/mod.js");
function result($ok, $err) {
    if ($ok.metadata.some((x) => x.factory === result)) {
        throw new Error("Nested result shape will not roundtrip correctly");
    }
    return (0, mod_js_1.createShape)({
        metadata: (0, mod_js_1.metadata)("$.result", result, $ok, $err),
        staticSize: 1 + Math.max($ok.staticSize, $err.staticSize),
        subEncode(buffer, value) {
            if ((buffer.array[buffer.index++] = +(value instanceof Error))) {
                $err.subEncode(buffer, value);
            }
            else {
                $ok.subEncode(buffer, value);
            }
        },
        subDecode(buffer) {
            switch (buffer.array[buffer.index++]) {
                case 0: {
                    const value = $ok.subDecode(buffer);
                    if (value instanceof Error) {
                        throw new mod_js_1.ShapeDecodeError(this, buffer, "An ok value that is instanceof Error will not roundtrip correctly");
                    }
                    return value;
                }
                case 1: {
                    return $err.subDecode(buffer);
                }
                default: {
                    throw new mod_js_1.ShapeDecodeError(this, buffer, "Result discriminant neither 0 nor 1");
                }
            }
        },
        subAssert(assert) {
            if (assert.value instanceof Error) {
                $err.subAssert(assert);
            }
            else {
                $ok.subAssert(assert);
            }
        },
    });
}
exports.result = result;
//# sourceMappingURL=result.js.map