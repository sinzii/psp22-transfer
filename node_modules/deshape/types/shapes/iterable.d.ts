import { AssertState, Shape } from "../common/mod.js";
export declare function iterable<TI, I extends Iterable<TI>, TO = TI, O = I>(props: {
    $el: Shape<TI, TO>;
    calcLength: (iterable: I) => number;
    rehydrate: (iterable: Iterable<TO>) => O;
    assert: (this: Shape<I, O>, assert: AssertState) => void;
}): Shape<I, O>;
