import { AssertState, Shape } from "../common/mod.js";
export declare function transform<TI, UI, TO = TI, UO = UI>(props: {
    $base: Shape<TI, TO>;
    encode: (value: UI) => TI;
    decode: (value: TO) => UO;
    assert?: (this: Shape<UI, UO>, assert: AssertState) => void;
}): Shape<UI, UO>;
